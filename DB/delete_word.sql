delete from words where w_id = $1;
select * from words where user_id = $2

-- knight	The opposite of day. Also fights dragons.
-- potato	What french fries are made of.
-- soda	A type of drink.
-- unique	Never the same.
-- cardboard	A type of box.


-- insert into words (name, definition,user_id)
-- values (${name}, ${definition},${user_id})